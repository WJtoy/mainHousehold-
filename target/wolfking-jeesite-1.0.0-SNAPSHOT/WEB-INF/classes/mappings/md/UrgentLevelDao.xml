<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wolfking.jeesite.modules.md.dao.UrgentLevelDao">
    <!--
    <sql id="columns">
            a.id,
            a.label,
            a.charge_in,
            a.charge_out,
            a.create_date,
            a.sort,
            a.mark_bgcolor,
            a.remarks
    </sql>
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO md_urgent_level(
			label,
			charge_in,
			charge_out,
			sort,
			mark_bgcolor,
			create_by,
			create_date,
			remarks
		) VALUES (
			#{label},
			#{chargeIn},
			#{chargeOut},
			#{sort},
			#{markBgcolor},
			#{createBy.id},
			#{createDate},
			#{remarks}
		)
    </insert>

    <update id="update">
        UPDATE
            md_urgent_level
        SET
            label= #{label},
            sort = #{sort},
            charge_in = #{chargeIn},
            charge_out = #{chargeOut},
            mark_bgcolor = #{markBgcolor},
            remarks = #{remarks},
            update_by = #{updateBy.id},
            update_date = #{updateDate}
        WHERE
            id = #{id}
    </update>

    <delete id="delete">
        UPDATE
            md_urgent_level
        SET
            del_flag = 1,
            update_by = #{updateBy.id},
            update_date = #{updateDate}
        WHERE
            id = #{id}
    </delete>

    <select id="get" resultType="UrgentLevel">
        SELECT
               <include refid="columns"/>
        FROM
               md_urgent_level a
        WHERE
                a.id = #{id}
          AND   a.del_flag = 0
    </select>


    <select id="findAllList" resultType="UrgentLevel">
        SELECT
            <include refid="columns"/>
        FROM
               md_urgent_level a
        WHERE
            a.del_flag = 0
        ORDER BY a.create_date
    </select>
    -->
    <!-- 分页用 -->
    <!--
    <select id="findList" resultType="UrgentLevel">
        SELECT
            <include refid="columns"/>
        FROM
              md_urgent_level a
        <where>
            a.del_flag = 0
        </where>
        ORDER BY a.sort,id
    </select>
    -->
</mapper>