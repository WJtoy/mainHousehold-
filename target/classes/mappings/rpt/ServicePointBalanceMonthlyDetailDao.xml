<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wolfking.jeesite.modules.rpt.dao.ServicePointBalanceMonthlyDetailDao">

	<insert id="incrBalance" parameterType="ServicePointPayableMonthlyDetail">
        INSERT INTO rpt_servicepoint_balance_monthly_detail (
				id,
				servicepoint_id,
				payment_type,
				`year`,
				product_category_id,
				${monthField}
		) VALUES (
				#{id},
				#{servicePoint.id},
				#{paymentType},
				#{year},
				#{productCategoryId},
				#{amount}
		)
		ON DUPLICATE KEY
		UPDATE ${monthField} = ${monthField} + #{amount}
    </insert>

	<insert id="incrBalanceWithBalanceForUpdateBalanceDetail" parameterType="com.kkl.kklplus.entity.fi.servicepoint.ServicePointPayableMonthlyDetail">
        INSERT INTO rpt_servicepoint_balance_monthly_detail (
				id,
				servicepoint_id,
				payment_type,
				`year`,
				product_category_id,
				${monthField}
		) VALUES (
				#{id},
				#{servicepointId},
				#{paymentType},
				#{year},
				#{productCategoryId},
				#{amount}
		)
		ON DUPLICATE KEY
		UPDATE ${monthField} = ${monthField} + #{amount}
    </insert>

</mapper>