<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wolfking.jeesite.modules.sys.dao.OfficeRegionDao">
    <select id="getUserIdsByOfficeIdProductCategoryId" resultType="com.wolfking.jeesite.modules.sys.entity.User">
        select
        su.id,
        su.name
        from sys_user su
        left JOIN md_productcategory_user mpu ON su.id = mpu.user_id
        where su.office_id = #{officeId}
        and mpu.product_category_id = #{productCategoryId}
        and su.del_flag = 0
    </select>
</mapper>