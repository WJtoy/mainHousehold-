<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wolfking.jeesite.modules.sys.dao.DictDao">
    
	<select id="get" resultType="com.wolfking.jeesite.modules.sys.entity.Dict">
		SELECT 
			* 
		FROM sys_dict 
		WHERE id = #{id}
	</select>
	
	<select id="findList" resultType="com.wolfking.jeesite.modules.sys.entity.Dict">
		SELECT 
			* 
		FROM sys_dict 
		WHERE del_flag = #{DEL_FLAG_NORMAL} 
		<if test="type != null and type != ''.toString()">
			AND type = #{type}
		</if>
		<if test="description != null and description != ''.toString()">
			AND description LIKE 
					<if test="dbName == 'oracle'.toString()">'%'||#{description}||'%'</if>
					<if test="dbName == 'mssql'.toString()">'%'+#{description}+'%'</if>
					<if test="dbName == 'mysql'.toString()">CONCAT('%', #{description}, '%')</if>
		</if>
		<if test="label != null and label != ''.toString()">
			AND label LIKE
			<if test="dbName == 'oracle'.toString()">'%'||#{label}||'%'</if>
			<if test="dbName == 'mssql'.toString()">'%'+#{label}+'%'</if>
			<if test="dbName == 'mysql'.toString()">CONCAT('%', #{label}, '%')</if>
		</if>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''.toString()">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY type, sort, update_date DESC
			</otherwise>
		</choose>

	</select>
	
	<select id="findAllList" resultType="com.wolfking.jeesite.modules.sys.entity.Dict">
		SELECT 
			* 
		FROM sys_dict 
		WHERE del_flag = #{DEL_FLAG_NORMAL} 
		ORDER BY type, sort, update_date DESC
		limit 2000
	</select>
	
	<select id="findTypeList" resultType="java.lang.String">
		SELECT 
			type 
		FROM sys_dict 
		WHERE del_flag = #{DEL_FLAG_NORMAL} 
		GROUP BY type
		ORDER BY type
	</select>

	<select id="checkUpdate" resultType="com.wolfking.jeesite.modules.sys.entity.Dict">
		SELECT
			value,
			label,
			type,
			description,
			update_date,
			remarks
		FROM
			sys_dict
		WHERE
			type		= #{type}
		AND del_flag 	= 0
		LIMIT 1
	</select>
	
	<insert id="insert" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO sys_dict(
			value, 
			label, 
			type, 
			description, 
			sort, 
			create_by, 
			create_date, 
			update_by, 
			update_date, 
			remarks, 
			del_flag
		) VALUES (
			#{value}, 
			#{label}, 
			#{type}, 
			#{description}, 
			#{sort}, 
			#{createBy.id}, 
			#{createDate}, 
			#{updateBy.id}, 
			#{updateDate}, 
			#{remarks}, 
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE sys_dict SET 
			value = #{value}, 
			label = #{label}, 
			type = #{type}, 
			description = #{description}, 
			sort = #{sort}, 
			update_by = #{updateBy.id}, 
			update_date = #{updateDate}, 
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE sys_dict SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>