<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wolfking.jeesite.modules.sys.dao.NoticeDao">
    <!--
    //2020-11-23 注释  begin
    <sql id="noticeColumns">
          a.id,
          a.user_id,
          a.title,
          a.context,
          a.refer_id,
          a.refer_no,
          a.quarter,
          a.link,
          a.notice_type,
          a.create_date
    </sql>

    <select id="get" resultType="Notice">
        select
              <include refid="noticeColumns"/>
        FROM
              sys_notice a
        WHERE
              a.id = #{id}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_notice
        (
          user_id,title,context,refer_id,refer_no,quarter,link,notice_type,remarks,create_by,create_date,del_flag
        )
        VALUES
        (
          #{userId},#{title},#{context},#{referId},#{referNo},#{quarter},#{link},#{noticeType},#{remarks},#{createBy.id},#{createDate},#{delFlag}
        )
    </insert>

    <update id="update" >
        update
          sys_notice
        SET
          update_by = #{updateBy.id},
          update_date = #{updateDate},
          del_flag = #{delFlag}
        WHERE
          id = #{id}
    </update>
    // 2020-11-23 注释  end
    -->


    <!--逻辑删除-->
    <!--
    // mark on 2020-11-23 begin
    <delete id="deleteById">
        UPDATE
          sys_notice
        SET
          del_flag = 1
        WHERE
          id = #{id}
    </delete>
    // mark on 2020-11-23 end
    -->

    <!--<delete id="deleteById">-->
        <!--DELETE FROM-->
          <!--sys_notice-->
        <!--WHERE-->
          <!--id = #{id}-->
    <!--</delete>-->

    <!-- 按用户获得列表 -->
    <!--
    // mark on 2020-11-23 begin
    <select id="getListByUserID" resultType="Notice">
        SELECT
            <include refid="noticeColumns"/>
        FROM
            sys_notice a
        <trim prefix="WHERE" prefixOverrides="AND|OR">
            <if test="userId != null">
                AND a.user_id = #{userId}
            </if>
            <if test="delFlag != null">
                AND a.del_flag = #{delFlag}
            </if>
        </trim>
        ORDER BY
            a.create_date DESC
    </select>

    <select id="getNotReadLastId" resultType="java.lang.Long">
        select
          a.id
        FROM
          sys_notice a
        WHERE
          del_flag = 0
          and refer_id = #{referId}
          and user_id = #{userId}
          and notice_type = #{noticeType}
        LIMIT 1
    </select>
    // mark on 2020-11-23 end
    -->

</mapper>
